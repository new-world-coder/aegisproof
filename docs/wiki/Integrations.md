# Integrations

| Integration | Status |
|---|---|
| GitHub Actions (repo CI) | Shipped for this repo |
| Consumer Action | Planned — see issues |
| RiskLine import/emit | Experiment design — optional consumer |
| XERJ indexing | Optional discovery only — not a trust root |
| CycloneDX export | Spike planned |

AegisProof must not depend on RiskLine or XERJ for core verify.
